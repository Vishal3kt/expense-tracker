import { MapMonthsPipe } from './map-months.pipe';

describe('MapMonthsPipe', () => {
  it('create an instance', () => {
    const pipe = new MapMonthsPipe();
    expect(pipe).toBeTruthy();
  });
});
